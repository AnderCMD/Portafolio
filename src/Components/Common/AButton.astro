---
// Tipos de props
interface Props {
    href: string;
    Texto: string;
}

// Props
const { href, Texto } = Astro.props
---
<a href={href}>
	<button
		class='bg-sky-500 hover:bg-sky-600 text-white font-bold py-2 px-4 rounded-full w-full mt-4 transition-all'>
		{Texto}
	</button>
</a>
