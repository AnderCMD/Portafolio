---
// Layouts
import Layout from '../../layouts/Layout.astro';

// Lenguaje
import { useLocale } from '../../Hooks/useLocale';
const { t } = useLocale(Astro);
---

<Layout title={`AnderCMD | ${t('Nav.Posts')}`} />
